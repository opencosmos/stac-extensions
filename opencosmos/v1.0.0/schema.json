{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://opencosmos.github.io/stac-extensions/opencosmos/v1.0.0/schema.json",
  "title": "OpenCosmos General Extension",
  "description": "STAC OpenCosmos General Extension for DataCosmos satellite imagery products. Captures general product metadata common across collections; item-level and collection-level fields are defined separately. Payload-specific, processing and quality-assurance metadata live in dedicated companion extensions (opencosmos-qa, opencosmos-simera, opencosmos-processing, ...).",
  "type": "object",
  "required": ["stac_extensions"],
  "properties": {
    "stac_extensions": {
      "type": "array",
      "contains": {
        "const": "https://opencosmos.github.io/stac-extensions/opencosmos/v1.0.0/schema.json"
      }
    }
  },
  "oneOf": [
    {
      "$comment": "This is the schema for STAC Items.",
      "type": "object",
      "required": ["type", "properties"],
      "properties": {
        "type": {
          "const": "Feature"
        },
        "properties": {
          "$ref": "#/definitions/item_fields"
        }
      }
    },
    {
      "$comment": "This is the schema for STAC Catalogs and Collections.",
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": {
          "enum": ["Catalog", "Collection"]
        }
      },
      "allOf": [
        {
          "$ref": "#/definitions/collection_fields"
        }
      ]
    }
  ],
  "definitions": {
    "item_fields": {
      "type": "object",
      "properties": {
        "opencosmos:copyright": {
          "type": "string",
          "title": "Copyright",
          "description": "Copyright notice for the data.",
          "examples": ["© 2026 Open Cosmos", "© 2023 European Space Agency - ESA"]
        },
        "opencosmos:price": {
          "type": "string",
          "title": "Price",
          "description": "Price of the product. Stored as a string to preserve decimal precision.",
          "pattern": "^[0-9]+(\\.[0-9]+)?$",
          "examples": ["1655.18", "929.20"]
        },
        "opencosmos:price_currency": {
          "type": "string",
          "title": "Price Currency",
          "description": "ISO 4217 currency code for the price.",
          "minLength": 3,
          "maxLength": 3,
          "pattern": "^[A-Z]{3}$",
          "examples": ["USD", "EUR", "GBP"]
        },
        "opencosmos:data_area_km2": {
          "type": "number",
          "title": "Data Area (km²)",
          "description": "Total coverage area of the data in square kilometers.",
          "minimum": 0
        },
        "opencosmos:collection_type": {
          "type": "string",
          "title": "Collection Type",
          "description": "Type of collection containing this item.",
          "enum": ["nominal", "qa"]
        },
        "opencosmos:activity_id": {
          "type": "string",
          "title": "Activity ID",
          "description": "Identifier of the tasking/processing activity that produced the item. The sentinel value 'not_found' is used when the activity could not be resolved.",
          "examples": ["c0e73dd3-9146-4264-955d-bea209ac8df6", "not_found"]
        },
        "opencosmos:payloads": {
          "type": "array",
          "title": "Payloads",
          "description": "Payload(s) involved in producing the item.",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "integer",
                "title": "Payload ID",
                "description": "Identifier of the payload on the spacecraft."
              },
              "serial_number": {
                "type": ["integer", "string"],
                "title": "Serial Number",
                "description": "Serial number of the payload."
              },
              "session": {
                "type": "integer",
                "title": "Session",
                "description": "Acquisition session identifier for the payload."
              }
            },
            "additionalProperties": true
          }
        },
        "opencosmos:acquisition_type": {
          "type": "string",
          "title": "Acquisition Type",
          "description": "Purpose of the acquisition.",
          "examples": ["nominal", "calibration"]
        },
        "opencosmos:tasking_request_id": {
          "type": "string",
          "title": "Tasking Request ID",
          "description": "Identifier of the tasking request that led to the acquisition.",
          "examples": ["480c2743-69fc-4b8f-9e2d-77121867b09b"]
        },
        "opencosmos:session": {
          "type": "array",
          "title": "Session",
          "description": "Acquisition session(s) associated with the item, per payload.",
          "items": {
            "type": "object",
            "properties": {
              "payload_id": {
                "type": "integer",
                "title": "Payload ID",
                "description": "Identifier of the payload on the spacecraft."
              },
              "session_id": {
                "type": "string",
                "title": "Session ID",
                "description": "Acquisition session identifier for the payload."
              }
            },
            "additionalProperties": true
          }
        }
      },
      "additionalProperties": true
    },
    "collection_fields": {
      "type": "object",
      "properties": {
        "opencosmos:resolution": {
          "type": "string",
          "title": "Resolution",
          "description": "Resolution level served for this collection. 'full' exposes native resolution assets, 'limited'/'preview' expose reduced-resolution assets.",
          "enum": ["full", "limited", "preview"]
        },
        "opencosmos:high_resolution_read_permission": {
          "type": "boolean",
          "title": "High Resolution Read Permission",
          "description": "Indicates whether the requesting user has permission to access high-resolution data."
        }
      },
      "additionalProperties": true
    }
  }
}
